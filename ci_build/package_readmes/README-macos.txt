RUNNING THE BRIDGE
==================

To run the MIDI<->Serial bridge, just extract the application (ideally
to the /Applications folder but you can put it anywhere) and then
double-click it.

The application requires macOS (TBD!)
